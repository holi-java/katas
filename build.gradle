allprojects {
  group 'com.holi'
  version '1.0'
}

subprojects {
  apply plugin: 'java'
}

apply plugin: 'idea'

idea.module.excludeDirs += ['.idea', '.gradle', 'gradle'].collect { return file(it); };