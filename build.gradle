group 'com.holi'
version '1.0'

subprojects{
  apply plugin:'java'

  repositories{
    mavenCentral()
  }

  dependencies{
    testCompile 'junit:junit:4.12'
  }
}

apply plugin: 'idea'

idea.module.excludeDirs += ['.idea', '.gradle', 'gradle'].collect { return file(it); };
